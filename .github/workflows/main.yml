on: [workflow_dispatch]

jobs:
  boman_scan:
    runs-on: ubuntu-latest
    name: Test Job
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Boman
        run: |
          sudo pip install boman-cli==1.5
          sudo boman-cli -a run -fb only-critical
#       - name: Upload boman result raw file
#         uses: actions/upload-artifact@v3
#         with:
#           name: homework
#           path: boman*
