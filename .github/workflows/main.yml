on: [workflow_dispatch]

jobs:
  boman_scan:
    runs-on: ubuntu-latest
    name: Test Job
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Boman
        run: |
          sudo pip install coloredlogs==15.0.1
          sudo pip install docker==5.0.3
          sudo pip install xmltodict==0.12.0
          sudo pip install --index-url=https://test.pypi.org/simple/ boman-cli-uat==12.3
          sudo boman-cli-uat -a run -u https://devapi.boman.ai/v2/
#       - name: Upload boman result raw file
#         uses: actions/upload-artifact@v3
#         with:
#           name: homework
#           path: boman*
